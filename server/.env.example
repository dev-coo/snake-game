# 서버 포트
PORT=3001

# 호스트 설정 (0.0.0.0은 모든 네트워크 인터페이스에서 접속 허용)
HOST=0.0.0.0

# 클라이언트 URL (CORS 설정용, 비워두면 모든 origin 허용)
# CLIENT_URL=http://localhost:3000

# Redis 설정 (선택사항)
# REDIS_URL=redis://localhost:6379

# 환경 설정
NODE_ENV=development